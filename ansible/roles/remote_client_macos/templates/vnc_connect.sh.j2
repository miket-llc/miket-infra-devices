#!/bin/bash
# VNC connection helper script for macOS
# Usage: vnc HOSTNAME [PORT]

HOST="${1:-motoko}"
PORT="${2:-5900}"

# Use short hostname - Tailscale MagicDNS handles resolution via search domains
TARGET="${HOST}:${PORT}"

# Use built-in Screen Sharing app
open "vnc://${TARGET}"

