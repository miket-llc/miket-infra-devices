#!/bin/bash
# Wrapper script for tailnet CLI tools
# Auto-generated by Ansible

VENV_PYTHON="{{ tailnet_cli_venv_path }}/bin/python"
REPO_PATH="{{ tailnet_cli_repo_path }}"

export PYTHONPATH="${REPO_PATH}:${PYTHONPATH}"
exec "$VENV_PYTHON" -m tools.cli.tailnet "$@"

