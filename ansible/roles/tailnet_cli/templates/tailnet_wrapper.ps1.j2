# Wrapper script for tailnet CLI tools
# Auto-generated by Ansible

$VENV_PYTHON = "{{ tailnet_cli_venv_path }}\Scripts\python.exe"
$REPO_PATH = "{{ tailnet_cli_repo_path }}"

$env:PYTHONPATH = "$REPO_PATH;$env:PYTHONPATH"
& $VENV_PYTHON -m tools.cli.tailnet $args

