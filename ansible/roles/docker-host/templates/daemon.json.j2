{
  "data-root": "{{ docker_data_root }}",
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "{{ mcp_log_max_size }}",
    "max-file": "{{ mcp_log_max_file }}"
  },
  "default-address-pools": [
    { "base": "10.98.0.0/16", "size": 24 }
  ]
}

