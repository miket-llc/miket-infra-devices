# Copyright (c) 2025 MikeT LLC. All rights reserved.

---
- name: Mount Network Shares on count-zero (Autofs)
  hosts: count-zero
  gather_facts: true  # Need facts for ansible_user, ansible_env, etc.
  become: true  # Required for autofs configuration (modifying /etc/auto_master, /etc/auto.motoko)
  become_method: sudo
  
  roles:
    - mount_shares_macos_autofs

