# Copyright (c) 2025 MikeT LLC. All rights reserved.

---
# Playbook: remote_clients.yml
# DEPRECATED: Use remote_clients_nomachine.yml instead
# This playbook is kept for historical reference only.
# NoMachine is the sole remote desktop solution (RDP/VNC retired 2025-11-22)

- name: Display deprecation notice
  hosts: localhost
  gather_facts: false
  tags:
    - remote
    - remote:client
  tasks:
    - name: Playbook deprecated
      ansible.builtin.debug:
        msg:
          - "=========================================="
          - "PLAYBOOK DEPRECATED"
          - "=========================================="
          - ""
          - "⚠️  This playbook is deprecated"
          - "RDP and VNC clients are no longer installed"
          - "NoMachine is the sole remote desktop solution"
          - ""
          - "Use instead:"
          - "  ansible-playbook -i inventory/hosts.yml playbooks/remote_clients_nomachine.yml"
          - ""
          - "Reference: docs/product/NEXT_INITIATIVE_PROMPT.md"

